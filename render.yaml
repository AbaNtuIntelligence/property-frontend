# render.yaml
services:
  - type: web
    name: property-frontend
    runtime: static
    buildCommand: npm install && npm run build  # ← Added npm install
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://rental-backend-1-263v.onrender.com
      - key: NPM_CONFIG_FROZEN_LOCKFILE
        value: false